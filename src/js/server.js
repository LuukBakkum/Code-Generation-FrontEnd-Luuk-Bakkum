// const express = require('express');
// const cors = require('cors');
// const app = express();

// app.use(cors({
//   origin: 'http://localhost:8080'
// }));

// app.get('/transactions', function(req, res) {
//   // code to retrieve transactions from database or external API
//   // and send them back in the response
// });

// other API endpoints here

// Save the above code in a file named server.js, and then start the server by running node server.js from the command line.

// Finally, run the app.js file by typing node app.js from the command line. 
//This will make a GET request to the http://localhost:3000/api/data endpoint, retrieve the data returned by the API, and log it to the console.

// In this example, we are using the axios library to make a GET request to a local API endpoint hosted on localhost:3000. The response.data property is then logged to the console.