// Sure, here's an example of retrieving data from a local API endpoint hosted on localhost using Node.js:

// Create a new file called app.js in your project directory and add the following code to it:

// const express = require('express');
// const cors = require('cors');
// const app = express();

// app.use(cors({
//   origin: 'http://localhost:8080',
//   methods: ['GET', 'POST', 'PUT', 'DELETE'],
//   allowedHeaders: ['Origin', 'X-Requested-With', 'Content-Type', 'Accept']
// }));

  //Save the app.js file, and then start a local server on localhost:3000 that serves an API endpoint that returns some data. For example, using Node.js and Express: